AUDITORIA COMPLETA — BestPriceToday Frontend
vc esta proibido de mudar apagar recortar .env do codigo 

    🟡 ALTO UX / BUGS VISÍVEIS
    ───────────────────────────
    
    5. Perfil page — carrega alertas com owner_id no POST response só
       perfil/page.tsx:40-46: Busca alertas sem owner_id, espera data.items. A API de alertas pode retornar formato diferente (array direto vs {items: [...]}). Se vier array direto, alerts fica vazio silenciosamente. → Tratar ambos os formatos.
    
    6. loadSource no VideoPublisher não executa no mount
       admin/page.tsx:223: React.useEffect(() => { loadSource(productSource); }, [topProducts]) — depende só de topProducts. Quando o componente monta, topProducts pode ser [] (ainda não carregou), então loadSource("top_clicks") roda mas faz setSourceProducts(topProducts.slice(0,10)) → vazio. Só carrega quando topProducts mudar depois. → Adicionar efeito separado para mount ou incluir productSource nas deps.
    
    7. offer.image || offer.image_url no TikTokPublisher
       TikTokPublisher.tsx:241: Offer não tem prop image, só image_url. O fallback nunca funciona. → Remover offer.image ||.
    
    8. Dashboard — border: "1px solid #1e293b" em cards claros
       dashboard/page.tsx vários lugares (linhas 165, 175, 273): fundo #ffffff + border #1e293b (preto) = borda fantasma. Algumas cards têm, outras têm rgba(108,92,231,0.2). → Padronizar borders claros.
    
    9. Privacy page — borda entre linhas da tabela borderBottom: "1px solid rgba(255,255,255,0.05)"
       privacy/page.tsx:81: Em fundo claro, essa borda é invisível. → Trocar pra "rgba(108,92,231,0.1)".
    
    10. Product page escura — borderBottom: "1px solid #1e293b" no header
        produto/[query]/page.tsx:42: Header com border escura em layout claro. → Trocar pra "rgba(108,92,231,0.2)".
    
    11. defaultAdmin color in dashboard tabs com contraste ruim
        alertas/page.tsx:203: Botão ativo tem background: "#7c6aff" com color: "#fff" — ok. Mas inativo tem rgba(255,255,255,0.04) — invisível no bg claro. → rgba(108,92,231,0.1) no inativo.
    
    🟠 SEGURANÇA
    ────────────
    
    12. JWT sem expiração checada no frontend
        O bpt_token é armazenado em localStorage e nunca validado por expiração no frontend. Se o token expirar, as requests falham silenciosamente com 401. O usuário só percebe que algo está errado quando tenta ação. → Checar exp do JWT e redirect pro login.
    
    13. Admin key em localStorage sem TTL
        admin_key fica salvo sem validade. Se alguém acessar o computador desbloqueado, pega a chave. Sem urgência total mas é risco. → Considerar sessionStorage ou TTL.
    
    14. openTrackedOffer abre popup sem verificação de erros
        Se a API retornar {error: ...}, o popup já foi aberto com about:blank e pode ficar em branco confundindo o usuário. → Abrir popup só depois de ter URL válida.
    
    🟢 BAIXO / CÓDIGO LIMPO
    ──────────────────────
    
    15. SearchesChart e StatsCards usam mockData (dados falsos)
        Sem impacto direto, mas se é pra parecer real, deveria buscar dados reais da API. Pelo menos está documentado como mock.
    
    16. README dos meus_links no dashboard pode nunca aparecer
        dashboard/page.tsx:70-72: Busca links com admin_key vazio "" — sempre retorna erro/0. A seção de links raramente mostra dados. → Remover ou correcionar a autenticação.
    
    17. monitor no loadSource do admin — useEffect faltando productSource nas deps
        Linha 223: ESLint/React warn sobre deps incompletas. loadSource e productSource não estão nas deps. Pode causar stale closure.
    
    18. VideoPublisher polla a cada 3s pra sempre se job falhar
        Se o job falhar no backend, o polling nunca para. → Adicionar timeout (ex: 5 min) ou max retries.
    
    19. Sem Content-Security-Policy nos headers
        Next.js não tem CSP configurada. Scripts inline (<style> no alertas page) podem ser bloqueados em ambientes restritos. → Configurar CSP em next.config.js em produção.
    
    20. seed_affiliate.py tem URL hardcoded do HF Space
        backend/seed_affiliate.py:44 → "https://aletubegames-bestpricetoday.hf.space" — se mudar o deploy, quebra. → Ler de env var.
